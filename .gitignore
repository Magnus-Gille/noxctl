.DS_Store
node_modules/
dist/
__pycache__/
*.js.map
*.d.ts.map
.env
credentials.json
credentials.dpapi
~/.fortnox-mcp/
PLAN.md
# Local Claude Code config stays out of the repo, but the skills directory is
# shared project content — it only lives here because that is where Claude Code
# looks; the originals are in skills/ and these are symlinks to them.
.claude/*
!.claude/skills/
debate/
.playwright-mcp/

# Fortnox's OpenAPI spec is its "call structure" (Developer Agreement cl. 6.1/6.3)
# — not redistributed in this repo. Fetched on demand into this local cache by
# scripts/check-api-changes.sh; drift is tracked via api-spec/openapi-fingerprint.json.
api-spec/openapi.json
